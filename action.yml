name: 'Git Metrics'
description: 'Extract git metrics for the code repository'
runs:
  using: 'docker'
  # image: 'Dockerfile'
  image: 'docker://avaliasystems/gh-actions-git-metrics:v0.1.2'
  args:
    - ${{ inputs.who-to-greet }}